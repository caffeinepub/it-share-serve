{
  "frontend/src/pages/ProfilePage.tsx": "Dark neon profile page with gradient header, animated heading, and media tabs; passes `photos`/`videos` arrays and `isLoading` props to `PhotoGallery`/`VideoGallery` (matching their existing prop interfaces).",
  "frontend/src/components/ImageGenerator.tsx": "AI image generator with a three-source fallback chain; now includes a prominent \"Buy This Design\" button that stores the image URL and prompt in sessionStorage and navigates to the customize-design flow before any purchase is completed.",
  "frontend/src/components/VideoGenerator.tsx": "AI video generator using a comprehensive keyword-to-MP4 mapping (50+ entries across diverse categories) with Mixkit direct URLs; extracts keywords from the prompt to find matching videos, falls back to a general pool, and cycles through alternatives on video error — no Pollinations or Pexels API calls.",
  "frontend/src/pages/CreatePage.tsx": "Create page with DailyChallengeWidget above the tabs, using a ref to programmatically set the ImageGenerator prompt when \"Use This Prompt\" is clicked.",
  "frontend/src/App.tsx": "Main app with TanStack Router; uses a `RootLayout` wrapper component that renders `<Layout><Outlet /></Layout>` as the root route component, fixing the children prop type error.",
  "frontend/src/hooks/useAuth.ts": "New authentication hook that manages username/password session state using localStorage for persistence, providing login, logout, isAuthenticated, and username values.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; fixes `useGetUserProfile` to call `getUserProfileByUsername(username)` instead of `getUserProfile(principal)` to match the correct backend method signature for string-based username lookups.",
  "frontend/src/pages/LoginPage.tsx": "Dark neon login page with gradient glow effects; navigates to `/` (root) on successful login instead of the non-existent `/home` route.",
  "frontend/src/pages/RegisterPage.tsx": "Dark neon registration page with gradient glow effects; navigates to `/` (root) on successful registration instead of the non-existent `/home` route.",
  "frontend/src/components/Layout.tsx": "App shell with dark neon sidebar navigation (desktop) and bottom nav (mobile); accepts `children` as a React prop and conditionally renders the nav only for authenticated non-public routes.",
  "frontend/src/pages/HomePage.tsx": "Welcome page with StoriesRow at the top, welcome banner, quick action cards, TrendingTopics section, and profile summary — all styled with the neon dark theme.",
  "frontend/src/pages/ContactsPage.tsx": "Contacts management page with SuggestedUsers widget at the top, followed by tabs for My Contacts, Requests, and Find People.",
  "frontend/src/pages/ChatPage.tsx": "Chat interface with contacts sidebar and conversation view; navigates to `/contacts` on back (instead of non-existent `/chat`), and uses `navigate({ to: '/chat/$username', params })` for contact selection.",
  "frontend/src/main.tsx": "App entry point with QueryClientProvider only — InternetIdentityProvider removed since auth is now handled by useAuth/localStorage.",
  "frontend/src/components/PhotoUploader.tsx": "Photo upload component that uses `useAuth` to get the current username and passes `{ username, blob }` to `useSharePhoto`, matching the updated mutation signature.",
  "frontend/src/components/VideoUploader.tsx": "Video upload component that uses `useAuth` to get the current username and passes `{ username, blob }` to `useShareVideo`, matching the updated mutation signature.",
  "frontend/src/components/MessageList.tsx": "Message list component updated to use `myUsername` (string) and `msg.senderUsername` instead of the old `msg.sender` Principal field, with `flex-shrink-0` replaced by `shrink-0`.",
  "frontend/src/pages/SearchPage.tsx": "Search page that reads the topic from `window.history.state` on mount (set by TrendingTopics via TanStack Router's state updater) to pre-populate the search query, with Photos and Videos tabs using Picsum and Mixkit fallback pools — no longer imports `useLocation`.",
  "frontend/src/hooks/useSettings.ts": "New custom hook that manages app settings (darkMode, notifications, autoplayVideos) using localStorage for persistence, with toggleSetting and updateSetting helpers.",
  "frontend/src/pages/SettingsPage.tsx": "New Settings page with labeled toggle switches for Dark Mode, Notifications, and Autoplay Videos, plus an About section — all styled with the neon dark theme and using the shadcn Switch component.",
  "frontend/src/components/PhotoGallery.tsx": "Photo gallery grid with per-item async URL resolution and a two-step Picsum Photos fallback chain on image load errors; lightbox view also has an onError fallback. Preserves loading skeletons and object URL cleanup.",
  "frontend/src/components/VideoGallery.tsx": "Video gallery grid with per-item async URL resolution and a multi-entry fallback array (Mixkit + Coverr direct MP4 URLs) cycled on video error; all videos render as inline HTML5 `<video>` elements with controls.",
  "frontend/src/pages/BrowserPage.tsx": "New in-app browser page with an iframe, address bar pre-populated with DuckDuckGo, Back/Forward/Refresh controls, URL-vs-search-query detection, and a loading indicator — all styled with the app's neon dark theme. Browser history is maintained client-side only.",
  "frontend/src/components/StoriesRow.tsx": "Horizontally scrollable stories row that fetches real registered users from the backend via `useGetAllUsers`, excludes the current logged-in user, derives initials and neon ring colors deterministically, shows loading skeletons while fetching, and shows an empty state if no other users exist — no hardcoded placeholder data.",
  "frontend/src/components/StoryViewerModal.tsx": "Full-screen story viewer modal updated to display the real user's `displayName` as the primary label and `@username` as a subtitle, with gradient background, animated progress bar, and Escape key support — no fake placeholder data.",
  "frontend/src/components/TrendingTopics.tsx": "Trending hashtag chips with neon colors; uses `state: { topic: tag } as any` to bypass the strict TanStack Router state type constraint.",
  "frontend/src/components/SuggestedUsers.tsx": "Suggested users widget that fetches real registered users from the backend, filters out the current user and existing contacts, and wires the Add button to the real `useSendContactRequest` mutation with inline loading/sent states — no hardcoded placeholder data.",
  "frontend/src/components/DailyChallengeWidget.tsx": "Daily challenge widget that rotates through 14 creative prompts based on the current day of year, with a \"Use This Prompt\" button that calls a callback to pre-fill the ImageGenerator.",
  "frontend/src/hooks/useNotificationCounts.ts": "Custom hook that returns unreadMessages (placeholder 0) and pendingRequests count derived from the useGetPendingContactRequests query.",
  "frontend/src/index.css": "Global styles with OKLCH color system, neon accent utilities, scrollbar-hide utility, story-progress keyframe animation, and Google Fonts import for Inter and Orbitron.",
  "frontend/tailwind.config.js": "Tailwind config with neon color tokens, Inter/Orbitron font families, and story-progress animation keyframe registered for use with Tailwind's animation utilities.",
  "frontend/src/components/DesignCustomizer.tsx": "Reusable design customization component with color palette selector, print size selector, finish type selector, quantity slider, and optional custom text input — all styled with the neon dark theme.",
  "frontend/src/pages/CustomizeDesignPage.tsx": "Customization page that loads a design from sessionStorage (keyed by designId route param), renders the DesignCustomizer, shows a live order summary with pricing, and navigates to the purchase confirmation page with customization data.",
  "frontend/src/pages/PurchaseConfirmationPage.tsx": "Purchase confirmation page that displays the design preview, all selected customizations, a full price breakdown (subtotal + shipping), and a \"Complete Purchase\" button that saves the design to the user's profile and shows a success state."
}